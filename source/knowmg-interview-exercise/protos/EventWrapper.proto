syntax = "proto3";
package hw;

import "Transaction.proto";

option java_package = "hw";
option java_multiple_files = true;

//A wrapper containing a series of transaction events
message EventWrapper {
  repeated Transaction events = 1; //an ordered list of transaction events
}
